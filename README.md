# Expandable

Expandable is an example of automaticlly resizing UITextView and UITableView on a UIScrollView.
When typing in the UITextView it is automatially reszized to display all text. 
When adding or removing items from the UITableView, it is resized to the size of displayed cells.
It also displays deleting and rearranging table cells.
